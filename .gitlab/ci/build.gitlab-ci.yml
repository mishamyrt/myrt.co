generate static files:
  stage: build
  cache:
    key: ${CI_COMMIT_REF_SLUG}-npm
    paths:
      - node_modules/
  variables:
    PARCEL_WORKERS: 1
    NODE_ENV: production
  before_script:
    - npm install
  script:
    - npm run build
  artifacts:
    paths:
      - dist
